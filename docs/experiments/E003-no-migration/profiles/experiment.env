# Общие параметры эксперимента (клиент usbip)
USBIP_HOST="192.168.1.149" # сервер usbip
USBIP_BUSID="3-2"          # busid экспортируемого девайса (на сервере usbip list -l)

DEV="/dev/sdb1"
MOUNTPOINT="/mnt/usb"

RUNTIME_SEC=120  # общая длительность fio
INJECT_AT_SEC=30 # когда применяем сбой после старта fio

# сетевой интерфейс клиента, который будем "ронять"
CLIENT_IFACE="enp0s3"

# для route_blackhole: адрес сервера usbip (можно тот же USBIP_HOST)
TARGET_HOST="${USBIP_HOST}"

# результаты
RESULTS_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)/results"
